using UnityEngine;

public class EventOnlyInteractable : Interactable
{
    // This script does nothing on it's own
    // This script is picked up by the Interactable editor the "Interactable Editor" script creates
    // After the script is put on an object, Interactable Editor recognises it and adds code
}
